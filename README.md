# prisma-reproductions

Variants:
1. Supply Prisma schema, then: create db, migrate, seed some data, run query against it
2. Supply SQL, then: create db, run SQL, introspect for schema, run query against it
